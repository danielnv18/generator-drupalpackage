language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-drupalpackage' ]; then cd .. && eval "mv $currentfolder generator-drupalpackage" && cd generator-drupalpackage; fi
